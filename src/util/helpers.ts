/**
 * Converts the provided data to a string representation.
 *
 * @param data - The value to be converted to a string.
 * @returns The string representation of the input data.
 */
export function makeString(data: unknown): string {
  return String(data).toString();
}
